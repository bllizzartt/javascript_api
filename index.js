// let today_day = prompt("Did I go to the gym today?");

// if(today_day = "yes") {
//     console.log('yes');
// }

var dice = {
    sides: 6,
    roll: function () {
      var randomNumber = Math.floor(Math.random() * this.sides) + 1;
      return randomNumber;
    }
  }
  console.log(randomNumber);

